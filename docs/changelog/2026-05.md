# Changelog — 2026-05

## 2026-05-06

- Synastry UI: responsive pass on form labels, dropdown text, metric rows, and daily energy axis headers to prevent narrow-device text overflow/orphan wrapping.
- Impacto arquitectónico: ninguno; cambio limitado a UI Compose y documentación de changelog.
- Pendientes: validar visualmente en dispositivos físicos pequeños con escalas de fuente altas y localizaciones largas.

## 2026-05-08

- Daily-limit UX: added a reusable paywall-style limit card and mapped expected daily/free/moon limit states in Birth essence, Synastry, Tarot, Oracle, and Pendulum away from red error UI; CTA copy points to the store while Premium is not implemented.
- Impacto arquitectónico: ninguno; cambio limitado a UI Compose, presentación, callbacks de navegación a MoonStore y strings localizados.
- Pendientes: validar visualmente el copy localizado en dispositivos estrechos cuando el flujo Premium definitivo esté disponible.

## 2026-05-11

- Premium subscriptions v1: added backend entitlement foundation for Google Play validation, receipt/index schemas, token hashing, callable contracts, mock validation mode and entitlement refresh semantics.
- Impacto arquitectónico: nuevo módulo backend `functions/src/premium` y nuevas colecciones backend-owned `/userEntitlements`, `/purchaseReceipts` y `/purchaseTokenIndex` para separar fuente comercial (Google Play) de fuente operativa (Firestore).
- Pendientes: configurar credenciales reales Google Play/Android Publisher, incorporar cifrado/KMS o RTDN para refresh server-side sin restore, y construir UI/flujo Android Billing en PRs posteriores.

## 2026-05-11 (PR 3 KMP/Android Billing boundary)

- Android/KMP Billing: `SubscriptionBillingDataSource` queda limitado a catálogo de productos y tokens restaurables; `GooglePlaySubscriptionBillingDataSource` devuelve `BillingProduct`, `BillingPurchaseToken` y `BillingPurchaseResult` sin convertir `PURCHASED` en Premium activo.
- Catálogo v1: se configura únicamente el producto mensual real `bwitch_premium_monthly`; anual permanece reservado y fuera del query/render de esta fase.
- Settings/analytics: una compra local exitosa o pending ya no dispara `premium_purchase_completed` ni refresca Premium desde Billing local; restore devuelve tokens para validación backend posterior y no concede Premium local.
- Impacto arquitectónico: se refuerza que `/userEntitlements/{uid}` y los callables backend son la única autoridad Premium; la cache legacy `subscription_status` solo puede leerse como estado informativo no autoritativo.
- Pendientes: conectar `purchaseToken` con `validateGooglePlaySubscription`/`restoreGooglePlayPurchases`, refrescar entitlement backend en UI, implementar UI Premium final y mantener fuera de scope moon packs/App Store/anual hasta sus PRs.

## 2026-05-11 (PR 4 Premium entitlement client wiring)

- Se añadió la capa cliente común de Premium Entitlements para invocar `validateGooglePlaySubscription`, `restoreGooglePlayPurchases` y `refreshEntitlement` desde KMP.
- Settings ahora valida compras/restores locales con backend antes de marcar Premium activo o emitir analytics de compra completada.
- Los estados locales de Billing continúan siendo no autoritativos: tokens locales no activan Premium sin entitlement backend activo.
- Se añadieron analytics tipados para pending, restore y refresh de entitlement.
- Impacto arquitectónico: se incorpora `PremiumEntitlementRepository` como boundary backend-owned separado de `SubscriptionRepository`, que sigue limitado a Billing local/catalog/token discovery.
- Pendiente PR 5: conectar un hook limpio para refrescar Economy tras activación/restauración Premium validada por backend.
