# 2026-03

## 2026-03-03
- Implementado `tarotDraw` end-to-end en Firebase Functions v2.
- Añadidos: auth obligatoria, `requestId` obligatorio, idempotencia en `oracleRequests/{requestId}` y cuotas diarias por `oracleUserDaily/{dateIso}/users/{uid}`.
- Integradas reglas económicas (`FREE_DAILY`, `AD_UNLOCK`, `SUBSCRIPTION`) y modos `DEGRADED` / `EMERGENCY`.
- Persistencia de historial en `tarotReadings/{uid}/items/{readingId}` y métricas de proveedor en `llmUsageDaily/{dateIso}/providers/{provider}`.
- Extraído `buildRouter` a `functions/src/llm/buildRouter.ts` para reutilización y evitar ciclos.

Impacto arquitectónico:
- Sin cambios de módulos KMP. Cambio localizado en backend functions/oracle.

Pendientes:
- Validación server-side de rewarded ads (SSV).
- Reserva/budget hard cap avanzado para llm usage (si se decide endurecer más allá del helper actual).

## 2026-03-04
- App KMP: añadido flujo mínimo end-to-end de `TarotDraw` usando `FunctionsClient` (`TarotRepositoryImpl`, `TarotViewModel`, `TarotScreen`) con idempotencia por `requestId` y retry reutilizando el mismo `requestId`.
- Navegación: nueva destination `Tarot` accesible desde el Portal.
- DI: registro de `TarotRepository` en `dataKoinModule` y de `TarotViewModel` en `presentationModule`.

## 2026-03-12
- App KMP: implementado Oráculo V1 en app (domain/data/presentation/ui) con navegación desde Portal y pantalla dedicada `Oracle`.
- Oracle data: añadido `oracleAsk` con parse defensivo para `IN_PROGRESS` y `COMPLETED_SUCCESS`, validación de campos mínimos y mapeo de errores de negocio hacia UI.
- DI: registro de `OracleAskViewModel` y mantenimiento de `OracleDebug` como pantalla técnica independiente.

Impacto arquitectónico:
- Sin cambios de módulos ni backend; extensión mínima de Oracle siguiendo patrón existente de Tarot.

Pendientes:
- Afinar copy/i18n de mensajes de error y estados si se prioriza internacionalización de Oráculo.


## 2026-03-17
- App KMP: agregado onboarding obligatorio post-login para completar perfil mínimo antes de entrar al Portal.
- Navegación: nuevo destino `OnboardingProfile` y gate en `AppRoot` con flujo `AuthGate -> OnboardingProfile -> Portal` según estado de sesión y completitud de perfil.
- Perfil mínimo centralizado en helper de dominio (`username` + `birthDate` + `zodiacSign`) para evitar duplicación.
- Onboarding UI: nueva pantalla enfocada en username, fecha de nacimiento y avatar opcional, con guardado y derivación de signo zodiacal.
- Ajustado gate de `AppRoot` para resolver primero perfil inicial con `getUserProfile()` antes de decidir root y evitar navegación prematura/flicker por snapshots iniciales (`null`/vacíos).

Impacto arquitectónico:
- Sin cambios de módulos; extensión incremental en domain/presentation/composeApp.

Pendientes:
- Validación de unicidad real de `username` en backend.

## 2026-03-17 (Perfil/Ajustes)
- ComposeApp: separación de UX entre `ProfileScreen` (visual/read-only) y `SettingsScreen` (acciones editables limitadas).
- Navegación: nuevo destino `Settings` y flujo desde Perfil -> Ajustes manteniendo back stack simple.
- Perfil: ahora muestra únicamente avatar, `@username` y signo zodiacal; se eliminaron email y fecha de nacimiento de esa pantalla.
- Ajustes: se limita a cambio de avatar, refresh de perfil y cierre de sesión; ya no hay edición de `username` ni `birthDate` fuera del onboarding.

Impacto arquitectónico:
- Sin cambios de arquitectura modular; refactor de pantallas en `composeApp` y ajuste menor de navegación en `shared/presentation`.

Pendientes:
- Iteración futura de perfil identitario con nivel/XP, mazos y carta natal.

## 2026-03-18
- Astrología: replanteado módulo `BirthChart` como V1 de **Esencia natal** con input manual de Sol/Luna/Ascendente y UX enfocada en interpretación breve.
- Dominio/Data: nuevo modelo de datos de esencia (`sunSign`, `moonSign`, `risingSign`, `archetype`, `interpretation`, `savedAtEpochMillis`, `updatedAtEpochMillis`) con soporte de una sola esencia activa por usuario.
- Backend Functions: añadido callable `birthEssenceGenerate` (LLM low-cost, salida corta y controlada) reutilizando router LLM existente.
- Persistencia: sincronización en `/users/{uid}/birthEssence/current` reemplazando la esencia activa al guardar una nueva.

Impacto arquitectónico:
- Sin nuevos módulos; reutilización de capas domain/data/presentation y Functions existente.

Pendientes:
- Afinar copy/i18n de labels de signos.
- Definir estrategia de compatibilidad/sinastría sobre el modelo de esencia.

## 2026-03-18 (ajuste de estabilización Birth Essence V1)
- Corregida compatibilidad con `ZodiacSign` en app KMP para usar enum real en minúsculas y evitar errores de compilación.
- Añadida integración mínima de producto entre esencia y perfil: al guardar esencia activa se persiste resumen en `profile/current.birthEssenceSummary` y se muestra en `ProfileScreen`.
- Endurecida compatibilidad de parseo de signos (`valueOf(lowercase())`) para tolerar mayúsculas/minúsculas en datos remotos/legacy.

## 2026-03-24 (limpieza técnica final Birth Essence V1)
- ComposeApp: extraído helper visual compartido `BirthEssenceArchetype -> DrawableResource` y reutilizado en `BirthChartScreen` y `ProfileScreen` para eliminar duplicación.
- Presentation: `ProfileScreen` deja de observar `BirthChartRepository` directamente; la esencia guardada se expone desde `UserProfileViewModel/UserProfileUiState`.
- Data: limpieza de logs temporales de `SyncBirthChartRepository`, manteniendo solo trazas de fallo relevantes para diagnóstico.
