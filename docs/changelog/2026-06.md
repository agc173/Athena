## 2026-06-26 — Basic natal chart MVP UI polish

- ComposeApp: pulida la sección temporal de carta natal básica con jerarquía visual, agrupación por fecha/hora/offset, CTA claro y resultados en cards para Sol y Luna.
- UX: añadida validación local ligera para fecha, hora y offset UTC antes de permitir el cálculo, con mensajes amistosos y sin llamadas remotas ni persistencia.
- Alcance: se mantiene MVP local intencional; no se añade ascendente, backend, Firestore, Functions, LLM, modelos de dominio ni cambios al calculador.
- Impacto arquitectónico: ninguno; cambio acotado a UI Compose y documentación de sesión.

## 2026-06-26 — Local birth date/time UTC adapter

- Domain: añadido `BirthDateTimeLocal` como modelo público para fecha/hora local de nacimiento con offset UTC explícito en minutos, y conversión pura `toUtc()` hacia `BirthDateTimeUtc` usando `kotlinx.datetime`.
- Alcance v1: no se añade lookup de ciudad, base de datos de zonas horarias, UI, ascendente ni cambios en `BasicNatalChartCalculator`; la futura UI/geocoding decidirá cómo seleccionar el offset.
- Testing: añadidos tests common para offsets de Madrid en verano/invierno y rollovers de fecha UTC hacia atrás/adelante.
- Impacto arquitectónico: adaptador pequeño en `shared/domain`; sin cambios API, Firestore, DI, pantallas Compose ni cálculo Android existente.

## 2026-06-26 — Basic natal chart calculator foundation

- Domain: añadidos modelos públicos iniciales para carta natal básica (`BirthDateTimeUtc`, `NatalChartResult`, `ZodiacSign`) y conversión pura de longitud eclíptica a signo zodiacal con normalización de grados.
- Data Android: movido el uso de Astronomy Engine a un `BasicNatalChartCalculator` permanente en `shared/data/src/androidMain`, calculando solo Sol y Luna desde la fecha y hora UTC; ascendente y UI quedan fuera de alcance.
- ComposeApp Android: eliminado el smoke test temporal de arranque en `BWitchApplication` y retirada la dependencia directa de Astronomy Engine; la dependencia queda aislada en `shared/data/androidMain`.
- Impacto arquitectónico: cálculo separado de UI y app startup; sin cambios API, Firestore, pantallas Compose, llamadas remotas ni DI.

## 2026-06-24 — Firebase Functions connectivity pre-check hotfix

- Data/ComposeApp Android: añadido `ConnectivityChecker` compartido con implementación Android basada en `ConnectivityManager` y registro DI de plataforma para detectar ausencia de red validada antes de ejecutar callables Firebase.
- Functions: `GitLiveFunctionsClient` ahora corta antes de llamar a Firebase Functions y devuelve `ApiError.Network` cuando no hay conexión usable; el mapeo existente de excepciones Firebase se mantiene como fallback para no convertir todos los `INTERNAL` en errores de conexión.
- UX: se reutiliza el mapeo existente de error de conexión en Birth Essence; no cambian backend, prompts, economía, endpoints ni copy nuevo.
- Impacto arquitectónico: abstracción pequeña de conectividad en `shared/data`; sin cambios en contratos API/Firestore/modelos públicos.

## 2026-06-24 — Birth Essence error UX hotfix

- Presentation: la generación de Esencia natal ahora mapea errores `ApiError` a categorías UX estables para lunas insuficientes, límite diario, conexión, sesión caducada y problema temporal de ATHENA, sin exponer errores backend crudos.
- UI/localización: añadidos textos localizados para los nuevos errores y conservado el comportamiento de `DailyLimitPaywallCard` ocultando el mensaje inline de límite diario.
- Data/domain: añadido `ApiError.Network` y mapeo network-aware en el cliente de Firebase Functions para que modo avión/problemas de conectividad lleguen a la UI como error de conexión.
- Impacto arquitectónico: ninguno; no se cambian endpoints, Functions, prompts, reglas de economía ni límites.

# Changelog — 2026-06

## 2026-06-24 — Birth Essence offline connection mapping hotfix

- Data: reforzado `GitLiveFunctionsClient` para mapear códigos estables de Firebase Functions `UNAVAILABLE` y `DEADLINE_EXCEEDED`, además de causas offline/DNS/timeout de cliente, a `ApiError.Network`.
- UX: se conserva el mapeo existente de `ApiError.Network` en Birth Essence hacia el mensaje localizado de problema de conexión; errores `INTERNAL`/backend/LLM siguen usando el mensaje temporal de ATHENA.
- Impacto arquitectónico: ninguno; no se modifican backend, endpoints, prompts, economía ni límites.

## 2026-06-24 — Closed Testing birth date picker UX

- ComposeApp: reemplazado el selector de fecha de nacimiento compartido en onboarding y edición de perfil por ruedas verticales día/mes/año, manteniendo un único campo visible fuera del diálogo.
- Localización: eliminados literales hardcodeados en español del diálogo de fecha (`Día`, `Mes`, `Año`, `Cancelar`, `Aceptar`, título) y añadidas claves comunes para los idiomas soportados.
- Compatibilidad: el selector usa `LocalDate` y sigue enviando `LocalDate.toString()` (`YYYY-MM-DD`) a los ViewModels existentes, por lo que no cambia schema de perfil, API, Firestore ni lógica astrológica.
- Validación: el selector limita años/meses/días válidos, contempla años bisiestos y no permite seleccionar fechas futuras.
- Impacto arquitectónico: ninguno; bugfix de UX limitado a UI Compose compartida.

Pendientes:
- Validar manualmente en Android Closed Testing/emulador los flujos de onboarding y edición de perfil con sesión real.

## 2026-06-24 — Profile habits progress CTA consistency

- ComposeApp/Profile: reemplazado el botón full-width `Open Habits` de la tarjeta de progreso de hábitos por un icono CTA sutil alineado a la derecha, reutilizando el patrón visual de las tarjetas Arcana y Constelaciones.
- UX: se conserva el contenido de progreso existente (título, subtítulo, ciclos completados, insignias y estado no iniciado) y la navegación sigue usando `onOpenHabits` desde el nuevo icono accesible.
- Impacto arquitectónico: ninguno; cambio acotado a UI Compose compartida y sin cambios en lógica de hábitos, perfil, API, Firestore ni modelos públicos.

## 2026-06-11 — Android release R8 audit for Closed Testing

- ComposeApp Android release ahora activa R8/obfuscación (`isMinifyEnabled = true`), resource shrinking y el preset `proguard-android-optimize.txt` con un archivo app-specific mínimo `composeApp/proguard-rules.pro`; además el App ID de AdMob del manifest se resuelve mediante `${ADMOB_APP_ID}` en vez de quedar hardcodeado al valor de test.
- Añadida auditoría operativa en `docs/launch/android-release-r8-closed-testing-audit.md` con riesgos por SDK, puntos de reflection/serialización, mapping files y checklist manual release para Closed Testing.
- No se añaden reglas `-keep` globales ni cambios funcionales; las reglas nuevas preservan solo atributos de metadata/annotations/signatures para reducir riesgo de serialización/SDKs sin desactivar shrink/obfuscación.
- Validación Gradle intentada con `./gradlew :composeApp:assembleRelease`, bloqueada en este entorno antes de configurar el proyecto por resolución del plugin AGP `com.android.application:8.7.3`.
- Impacto arquitectónico: ninguno; cambio limitado a configuración de build Android release y documentación de lanzamiento.

Pendientes:
- Repetir `assembleRelease`/`bundleRelease` en el entorno Android real con AGP resoluble y archivar `composeApp/build/outputs/mapping/release/mapping.txt`.
- Ejecutar smoke manual release minificada para login Google/Firebase, FCM, Billing, AdMob, App Check, Storage avatar, callables y navegación principal antes de subir a Closed Testing.

## 2026-06-03 — Storage avatar rules hardening

- Versionadas reglas de Firebase Storage en `storage.rules` para avatares en `users/{uid}/profile/avatar_<epoch>.<ext>`.
- Añadida configuración `storage.rules` y emulador Storage en `firebase.json`, sin deploy automático.
- Endurecido contrato deny-by-default: lectura/borrado solo owner, escritura solo owner con nombre válido, MIME `image/jpeg`, `image/png` o `image/webp`, y tamaño máximo de 5 MB.
- Documentado contrato operativo, compatibilidad Android/iOS, pruebas manuales en Rules Playground/Emulator Suite y pasos de deploy manual en `docs/data/storage/rules.md`.
- Impacto arquitectónico: ninguno; cambio limitado a reglas Firebase Storage y documentación operativa. No cambia UI, rutas Storage, Firestore Rules ni flujo de avatar.

Pendientes:
- Ejecutar `firebase deploy --only storage --dry-run` si la Firebase CLI usada lo soporta; si no, validar con Emulator Suite/Rules Playground antes del deploy real.
- Validar en dispositivo Android si algún proveedor devuelve `image/jpg`; las reglas aceptan solo el MIME canónico `image/jpeg`.

## 2026-06-03 — Pre-closed testing data hygiene audit

- Documentada auditoría pre-closed testing en `docs/launch/pre-closed-test-data-hygiene.md` para retención/costes Firestore, usuarios Auth desconocidos y Storage avatars.
- Cubiertos hallazgos de reglas Firestore/Storage, protocolo seguro para `test@test.com` / `test@example.com`, checklist manual en Firebase Console y prompts futuros para TTL/cleanup.
- No se modifica código productivo, economía, UI, datos reales ni despliegues; la entrega es documental.
- Impacto arquitectónico: ninguno; documento operativo de lanzamiento/checklist.

Pendientes:
- Confirmar reglas reales de Firebase Storage en Console y versionarlas si no están estrictas.
- Validar manualmente UIDs, metadata Storage y colecciones Firestore asociadas antes de borrar/deshabilitar definitivamente cuentas u objetos.

## 2026-06-02 (ios-launch-preparation audit)
- Creada auditoría de preparación iOS sin Mac/Xcode en `docs/launch/ios-readiness.md`.
- Cubierto estado KMP/iOS, Firebase iOS, login, Premium/compras, push, share/recursos, permisos/políticas, QA con Mac y riesgos de lanzamiento.
- No se modifica código productivo ni economía; la entrega es documental.
- Validación Gradle intentada desde Linux, bloqueada por resolución de plugin AGP/dependencias del entorno antes de ejecutar tareas.

Impacto arquitectónico: ninguno; documento operativo de lanzamiento/checklist.

Pendientes:
- Validar build real en Mac/Xcode.
- Añadir configuración nativa iOS (`GoogleService-Info.plist`, signing, capabilities, privacy manifest/permisos).
- Decidir scope de TestFlight respecto a compras, push y login social iOS.

## 2026-06-02 — Profile description persistence fix

- Cliente KMP/Data: corregida la caché local de `SettingsUserProfileRepository` para serializar y restaurar `description` junto al resto de campos de `UserProfile`.
- Testing: añadido test common de repositorio local que guarda un perfil con descripción y lo recarga desde el mismo `Settings` store para cubrir la regresión al volver a Perfil.
- Auditoría: el callable `saveUserProfile`, el DTO callable, el DTO remoto Firestore y las reglas ya usan el campo `description` con límite 160; no hay cambio de contrato ni schema.
- Impacto arquitectónico: ninguno; bugfix acotado a persistencia/cache local de perfil. No cambia rutas Firestore, API, DI ni UI.
- Pendientes: validar en dispositivo/emulador con sesión real que `users/{uid}/profile/current.description` permanece tras guardar y navegar fuera/volver.

## 2026-06-02 — Taurus period horoscope generation fix

- Functions: weekly/monthly period horoscopes now normalize zodiac identity returned by the LLM before validation/writes, accepting canonical English slugs plus common Spanish labels/casing such as `Tauro` → `taurus`.
- Reliability: complete-doc checks now require the canonical sign slug for the expected Firestore path so localized/uppercase sign values are repaired instead of being skipped as complete.
- Admin/backfill: `backfillPeriodHoroscopes` now normalizes requested signs/langs and emits per-period/sign/lang result logs for canonical and translation phases.
- Impacto arquitectónico: ninguno; cambio limitado a helpers de identidad zodiacal, generación/backfill de Functions y logging operativo. No cambia contratos API, rutas Firestore ni economía.
- Pendientes: desplegar Functions, ejecutar backfill admin seguro para Tauro en períodos afectados y validar logs/resultados en Firestore.

## 2026-06-02 — Admin local period horoscope backfill

- Functions: añadido script admin local para ejecutar backfill weekly/monthly con Firebase Admin SDK y credenciales locales/ADC, sin exponer endpoint público nuevo.
- Backfill: la callable y el script comparten la misma rutina de generación, normalización de signos/langs, límites de períodos y contadores `created`/`skipped`/`repaired`/`failed`.
- Operativa: añadido comando npm `backfill:period` para invocar el script con `periodType`, `startKey`, `endKey`, `signs` y `langs` opcionales.
- Impacto arquitectónico: ninguno; cambio limitado a tooling/admin de Functions. No cambia contratos API, rutas Firestore, economía ni cliente.
- Pendientes: ejecutar el backfill real de Tauro weekly/monthly con credenciales admin y validar logs/resultados en Firestore.

## 2026-06-02 — Moon Store Premium price display

- ComposeApp Store: Moon Store now passes the first non-empty Premium `formattedPrice` from the existing subscription catalog into the shared Premium card, matching Settings display.
- Scope: no purchase, restore, entitlement, billing flow, API, Firestore, DI, or domain model changes.
- Impacto arquitectónico: ninguno; bugfix visual acotado a UI Compose.
- Pendientes: validar en dispositivo/emulador con catálogo real que Settings y Moon Store muestran el mismo precio y que el CTA Premium abre el flujo existente.

## 2026-06-03 — Avatar crop adjustment flow

- ComposeApp Android: el selector de avatar ahora abre un diálogo común del propio `AvatarPickerButton` con preview circular, pan y zoom antes de devolver el archivo a los ViewModels existentes.
- ComposeApp Android: la imagen confirmada se recorta a un JPG cuadrado temporal de 512x512 px antes de subir, manteniendo el límite operativo de 5 MB para la imagen elegida y los formatos JPG/PNG/WEBP.
- ComposeApp iOS: activada la edición nativa del `UIImagePickerController` y se sube la imagen editada como JPG temporal, reutilizando el mismo contrato `AvatarPickerButton`.
- ComposeApp Onboarding: la preview de avatar existente ahora también se recorta con `CircleShape` para alinearse con Perfil y navegación.
- No se cambian rutas Storage, reglas Storage, Firestore, backend, contratos de dominio ni ViewModels; onboarding y perfil siguen llamando al mismo componente y al mismo `uploadAvatarAndSave` existente.
- Impacto arquitectónico: ninguno; cambio acotado a UI/platform picker de `composeApp` y documentación de sesión.

Pendientes:
- Validar manualmente en dispositivos Android/iOS que los proveedores de galería devuelven MIME y tamaño correctamente, y que el recorte confirmado coincide con todos los avatares circulares mostrados por la app.
- Si se requiere una UX idéntica pixel-perfect en iOS y Android, sustituir la edición nativa iOS por un cropper Compose/iOS custom en una iteración posterior.

## 2026-06-03 — Avatar crop review fixes

- Movidos los textos visibles del cropper Android a `AppStrings.profile` con traducciones para ES/EN/PT/RU/FR/IT/DE, manteniendo `AvatarPickerButton` como punto único compartido por onboarding y perfil.
- Android ahora corrige la rotación EXIF básica (`90/180/270`) antes de mostrar y recortar la imagen seleccionada, evitando crops girados cuando la galería entrega bitmaps sin orientación aplicada.
- Revalidado el contrato Storage del cambio: el archivo final confirmado por Android se devuelve como `image/jpeg`, se genera a 512x512 px y mantiene la ruta existente `users/{uid}/profile/avatar_<timestamp>.<ext>` porque no se modifica `FirebaseAvatarRepository`.
- iOS conserva edición nativa (`allowsEditing`) y devuelve la imagen editada como `image/jpeg` sin cambiar el contrato común de `AvatarPickerButton`.

Pendientes:
- Validar build/runtime en un entorno con Android Gradle Plugin resoluble y con dispositivos reales Android/iOS.

## 2026-06-03 — Firestore retention policy audit

- Documentación: creada la política de retención/limpieza Firestore en `docs/data/firestore/retention-policy.md` como auditoría y diseño previo a Closed Testing.
- Auditoría: clasificadas colecciones con crecimiento por fecha, usuario, requestId y logs operativos; se diferenciaron rutas TTL posibles, dry-run manual y rutas críticas excluidas.
- Seguridad: no se implementó borrado real, no se activó TTL, no se modificaron reglas Firestore, no se desplegó backend y no se tocaron datos productivos.
- Impacto arquitectónico: ninguno; cambio exclusivamente documental bajo `docs/`. La decisión técnica futura queda propuesta, no aplicada.

Pendientes:
- Implementar, si se necesita, un script admin read-only de dry-run antes de cualquier cleanup real.
- Confirmar con producto/soporte qué historiales Oracle/Tarot/horóscopos desbloqueados deben ser visibles o restaurables.
- Añadir `expiresAt` únicamente en writes backend-owned allowlisted en una iteración futura y activar TTL solo tras dry-run y checklist operativo.

## 2026-06-03 — Test coverage audit pre Closed Testing

- Documentada auditoría de tests y coverage en `docs/qa/test-coverage-audit.md` antes de Google Play Closed Testing.
- Inventariados tests `commonTest`, selftests/scripts de Functions, ausencia de `androidUnitTest`/`androidInstrumentedTest`/`iosTest`, ausencia de tests UI/E2E y ausencia de tests automatizados de Firestore/Storage Rules.
- Coverage: no existe Kover/Jacoco ni coverage Gradle/npm versionado; solo se obtuvo una métrica diagnóstica parcial de Functions con `node:test` coverage que falló por 3 selftests rojos.
- Validación: `npm --prefix functions run lint` y `npm --prefix functions run build` pasan; Gradle queda bloqueado en este entorno por resolución de AGP 8.7.3 antes de ejecutar tests.
- Impacto arquitectónico: ninguno; cambio exclusivamente documental bajo `docs/`. No se modifica código productivo, lógica de tests, reglas, deploy ni configuración de build.

Pendientes:
- Resolver la ejecución Gradle en un entorno con AGP resoluble y ejecutar los tests KMP/Android.
- Corregir o actualizar las expectativas de los selftests Functions rojos de economía/runtime config.
- Añadir scripts npm oficiales para selftests/coverage y, antes de producción pública, Kover/CI/Firebase Rules tests.

## 2026-06-24 — Birth Essence requestId retry hotfix

- Cliente KMP/Presentation: corregido `BirthChartViewModel` para invalidar el `requestId` local tras errores terminales de generación de Esencia Natal, de modo que el siguiente retry de usuario cree un `requestId` nuevo.
- Se mantiene la reutilización del mismo `requestId` cuando el backend responde `IN_PROGRESS`/`PROCESSING`, preservando el polling/continuación idempotente del intento activo.
- Testing: añadidos tests common del ViewModel para cubrir retry con `IN_PROGRESS` y retry posterior a error terminal.
- Impacto arquitectónico: ninguno; hotfix acotado a estado de presentación y lifecycle de requestId. No cambia API, Functions, economía, prompts, Firestore ni lógica de rewards/deck unlock.
