## 2026-04-28 (analytics funnels v1: auditoría técnica y plan mínimo)
- Auditoría de eventos de monetización emitidos realmente (paywall, rewarded ads, premium, moon store, content unlock y module limits) cruzando contrato tipado domain + wiring de presentation/Compose.
- Matriz de reconstructibilidad de funnels objetivo: estado actual (sí/parcial/no) y límites por correlación/origen.
- Identificación de gaps concretos: inconsistencia placement/module, ausencia de correlation id transversal, unlock origin incompleto, premium CTA fuera de paywall y packs de store en coming soon.
- Propuesta mínima por commits para `analytics-funnels-v1` sin implementación productiva: params opcionales backward-compatible, archivos a tocar y tests de no-regresión.
- Nuevo informe: `docs/context/analytics-funnels-v1-audit.md`.

## 2026-04-28 (Economy v1.0 final: source-of-truth alignment backend/shared/UI)
- Functions/economy: catálogo consolidado al modelo final cerrado (`TAROT_1` premium hard cap `5/día`; `SYNASTRY` tope técnico `30/día` para free/premium).
- Functions/economy resolvers:
  - `TAROT_1`: premium incluido no cae a gasto moon; al agotar incluido responde `REJECT` por límite premium.
  - `ORACLE_1Q`: límite de uso moon `10/día` también aplica a premium cuando usa extras moon.
  - Horóscopo unlocks (daily/weekly/monthly): resolución centralizada para que premium incluido no dependa de balance moon.
- Shared/domain: `MoonUnlockCostCatalog.HoroscopeFutureDay` corregido a `1` Luna.
- Shared/data + presentation fallback horóscopo: weekly/monthly fallback corregidos a `2/3` (antes heredaban `1`).
- Tests backend (selftest): cobertura de prioridad `FREE > PREMIUM_INCLUDED > MOON > REJECT`, premium incluido independiente de moon balance, horóscopo premium sin cargo moon y rewarded ads (`+1`, max `3/día`).
- Documentación: nuevo contexto `docs/context/economy-v1-final.md` con tabla Free/Lunas/Premium y reglas globales.

## 2026-04-27 (Functions: auditoría variabilidad horóscopo diario v2)
- Se auditó el pipeline daily actual (prompt, generator, scheduler y schema Firestore) para identificar causas de repetición por signo.
- Se documentó propuesta técnica v2 de bajo coste sin tocar UI ni economía: seed determinista (`sign|dateIso|daily-v2`), `dailyAngle` derivado, historial compacto del mismo signo (N=1 recomendado), blacklist de clichés y validación ligera anti-repetición.
- Se mantuvo explícitamente el enfoque de canónico ES + traducciones para preservar consistencia semántica entre idiomas.
- No se aplicaron cambios productivos en Functions en esta iteración; se dejó plan y diff orientativo para implementación segura posterior.

## 2026-04-23 (KMP: economy balance source-of-truth consolidation incremental)
- Data (moons): nuevo `BackendFirstMoonRepository` como implementación principal de `MoonRepository` con política explícita **backend-first + fallback local** para lectura/observación de saldo.
- Data (DI): `SettingsMoonRepository` queda registrado como soporte legacy local y `MoonRepository` ahora se resuelve con el wrapper backend-first, sin migración masiva de módulos.
- Presentation (Tarot): `refreshMoonBalanceFromBackend()` deja el ajuste por delta ad-hoc y pasa a refrescar saldo vía `GetMoonBalanceUseCase` (alineado con la nueva política de repositorio).
- Compatibilidad temporal: se mantienen caminos legacy locales (`addMoons`/`spendMoons`) para fallback y compat mientras conviven modos backend v2/legacy.
- Tests: nuevos tests para `BackendFirstMoonRepository` y actualización de tests de `TarotViewModel` para validar que el refresh de TAROT_3 ya no aplica ajustes delta locales.

## 2026-04-22 (Functions: birth essence economy v2 bajo flag backend)
- `birthEssenceGenerate` ahora soporta dual-path: legado intacto y economía v2 detrás de `config/economy/current.birthEssenceEconomyV2Enabled`.
- Nuevo resolver transaccional en `functions/src/economy/birthEssenceEconomy.ts` con reserva/complete/refund e idempotencia por `economyRequests/{uid}/requests/{requestId}`.
- Resolución económica server-side para `BIRTH_ESSENCE` con prioridad `FREE > PREMIUM_INCLUDED > MOON > REJECT`, aplicando reglas oficiales (free lifetime único, premium incluido mensual, extras moon y límite diario total).
- Counters/flags de Birth Essence ahora se aplican y revierten de forma explícita en backend: `birthEssenceTotalUsed`, `birthEssenceMoonUsed`, `birthEssencePremiumExtraMoonUsed`, `birthEssencePremiumIncludedUsed` (monthly) y `birthEssenceFreeClaimed` (lifetime).
- Se añade/usa trazabilidad de ledger para gasto moon `BIRTH_ESSENCE_MOON_SPEND` y compensación `REFUND` idempotente ante fallos post-reserva.

## 2026-04-22 (Functions: economy fase 5 `claimRewardedAd` backend)
- Nuevo callable `claimRewardedAd` en `functions/src/economy/callables/claimRewardedAd.ts`, exportado en `functions/src/economy/index.ts` y `functions/src/index.ts`.
- Flujo transaccional con auth obligatoria e idempotencia por `economyRequests/{uid}/requests/{requestId}`: retries con el mismo `requestId` devuelven la respuesta estable previa sin duplicar crédito.
- Rewarded ads: aplica recompensa `+1` Luna y límite diario `3/día` vía `economyUsageDaily/{dateIso}/users/{uid}.rewardedAdsClaimed`.
- Respuestas estables de negocio para app futura: `CLAIMED` y `DAILY_LIMIT_REACHED`, incluyendo `balance`, `rewardedAdsClaimed`, `rewardedAdsRemaining` y `dailyLoginClaimed`.
- Ledger: añadido movimiento `REWARDED_AD_CLAIM` en `economyBalances/{uid}/ledger/{entryId}` con `amount`, `requestId`, `dateIso` y `placement` opcional.
- Validación de proof en fase 5: `adProof` obligatorio con checks mínimos de estructura (string no vacío + límites de longitud), dejando preparado endurecimiento posterior con SSV/SDK.

## 2026-04-22 (Functions: oracle economy v2 bajo flag backend)
- `oracleAsk` ahora soporta dos caminos: legacy intacto y economía v2 detrás de `config/economy/current.oracleEconomyV2Enabled`.
- En modo v2, `ORACLE_1Q` resuelve elegibilidad server-side con prioridad `FREE > PREMIUM_INCLUDED > MOON > REJECT` usando `economyBalances`, `economyUsageDaily`, premium status e idempotencia por `economyRequests/{uid}/requests/{requestId}`.
- Añadido flujo transaccional de reserva/commit/refund para Oráculo en `functions/src/economy/oracleEconomy.ts`, incluyendo compensación idempotente de balance/counters si falla LLM o post-proceso.
- En v2, `rewardedProof` se mantiene aceptado por compatibilidad de request pero deja de gobernar el unlock económico de `oracleAsk`.
- Se añade ledger type `ORACLE_1Q_MOON_SPEND` y se comienzan a poblar los counters diarios `oracleFreeUsed`, `oraclePremiumUsed` y `oracleMoonUsed`.

## 2026-04-22 (Functions: tarot economy v2 bajo flag backend)
- `tarotDraw` soporta dos caminos: legacy intacto y economía v2 detrás de `config/economy/current.tarotEconomyV2Enabled`.
- En modo v2, la elegibilidad de `TAROT_1`/`TAROT_3` se resuelve server-side con prioridad estricta `FREE > PREMIUM_INCLUDED > MOON > REJECT`, usando balance + usage diario/semanal + premium status.
- Añadido flujo transaccional mínimo para Tarot en `functions/src/economy/tarotEconomy.ts`: reserva idempotente por `requestId`, cargo Moon cuando corresponde, actualización de contadores y persistencia en `economyRequests`.
- Añadida compensación idempotente para fallos post-reserva: refund Moon (si hubo cargo), reversión de counters aplicados y trazabilidad en ledger (`REFUND`).
- En v2, `rewardedProof` se mantiene aceptado por compatibilidad de request pero deja de gobernar desbloqueo económico en Tarot.
- Docs Firestore actualizadas para reflejar flag runtime de economía y nuevos campos/estados en `economyRequests` + tipos de ledger Tarot/refund.

## 2026-04-22 (Functions: economy status final consistency pass)
- `getEconomyStatus`: `SYNASTRY` y `PENDULUM` exponen `moonCostPerUse=1` de forma explícita (además de `moonPackUsesPerMoon`) para evitar `undefined` accidental en snapshot.
- `getEconomyStatus`: `maxTotalRemaining` premium en `TAROT_3` y `ORACLE_1Q` ahora descuenta tanto consumos premium incluidos como consumos moon del día; si faltan contadores, el campo se omite y se informa en `notes`.
- Rules catalog: se añade `moonCostPerUse=1` en reglas pack-based (`SYNASTRY`, `PENDULUM`) para mantener consistencia del contrato.

## 2026-04-22 (Functions: economy fase 1 corrective pass)
- `getEconomyStatus`: snapshot ajustado para evitar remainings engañosos; se corrigen dependencias de contadores (p.ej. TAROT_1 premium no depende de uso free) y se omiten/cualifican campos cuando faltan contadores fiables en fase 1.
- `getEconomyStatus`: reducción de duplicación usando reglas base desde `rulesCatalog` para costes y límites.
- `claimDailyLogin`: eliminada mutación artificial `rewardedAdsClaimed: increment(0)`.
- Schema docs: añadido contador opcional `tarot1PremiumUsed` para soportar cálculo premium de TAROT_1 cuando esté disponible.

## 2026-04-22 (Functions: economy backend fase 1)
- Functions: nueva base de economía en `functions/src/economy/*` con tipos, helpers de paths Firestore, catálogo oficial de reglas y helper reutilizable de premium status (`userEntitlements/{uid}`).
- Functions: nuevos callables `getEconomyBalance`, `getEconomyStatus` y `claimDailyLogin` con auth obligatoria, timezone Europe/Madrid e idempotencia por `requestId` para claim diario.
- Functions: `claimDailyLogin` usa transacción Firestore para sumar +1 Luna una sola vez al día, actualizar usage diario, escribir ledger y persistir resultado idempotente en `economyRequests/{uid}/requests/{requestId}`.
- Functions: export de los nuevos callables en `functions/src/index.ts` sin tocar endpoints legacy existentes.
- Docs Firestore: documentadas colecciones base `economyBalances`, `economyUsageDaily/Weekly/Monthly`, `economyLifetime`, `economyRequests` y `economyUnlocks`.

Impacto arquitectónico:
- Se añade slice backend `economy` aislada, sin integración aún con `tarotDraw`, `oracleAsk`, `birthEssenceGenerate`, rewarded ads ni refunds.

Pendientes:
- Integrar resolver transaccional de consumo por módulo.
- Conectar economía con callables oracle/tarot/birth-essence.
- Implementar `claimRewardedAd` y refunds en iteraciones posteriores.

## 2026-04-21 (foundation Lunas: economía local + Tarot + Perfil/Tienda)
- Domain (moons): nuevo foundation de economía virtual con `MoonBalance`, `MoonPack`, `MoonRepository`, `MoonPackRepository`, `SpendMoonsResult` y catálogo centralizado de costes `MoonUnlockCostCatalog` (incluye `TarotExtraReading` y `HoroscopeFutureDay`).
- Domain (use cases): añadidos casos de uso mínimos para Lunas: obtener saldo, observar saldo, añadir, gastar, validar saldo suficiente y obtener catálogo de packs.
- Data (moons): nueva persistencia local `SettingsMoonRepository` con `multiplatform-settings` (saldo local y flujo observable), más `MockMoonPackRepository` para catálogo de packs mock/local.
- DI: registrados repositorios/casos de uso de Lunas en módulos Koin (`dataKoinModule`, `domainModule`, `presentationModule`) sin cambios de arquitectura.
- Presentation: `UserProfileViewModel` ahora observa saldo real de Lunas; nuevo `MoonStoreViewModel` para catálogo de packs mock + CTA placeholder; `TarotViewModel` integra primer consumo real de Lunas para tirada extra (TAROT_3) y deja punto preparado para recompensa futura de anuncio (`grantOneMoonFromFutureRewardedAd`).
- ComposeApp Perfil: bloque “Lunas” muestra saldo real desde estado en lugar de placeholder.
- ComposeApp Tienda: nueva pantalla `MoonStoreScreen` y destino de navegación `Destination.MoonStore` para listar packs mock y CTA “coming soon” sin compra real.
- ComposeApp Tarot: se muestra saldo actual y feedback claro cuando no hay saldo suficiente para tirada extra; no se muestran mensajes técnicos raw.
- Nota de negocio Tarot: el cobro de Lunas para tirada extra ocurre al iniciar `newRequest(TAROT_3)` porque ahí ya se lanza la llamada costosa al backend/LLM.
- Tests: añadidos tests unitarios de lógica de saldo/gasto en domain (`MoonUseCasesTest`) y actualizado test de `TarotViewModel` para nueva inyección de dependencia de Lunas.

Pendiente explícito de esta iteración:
- Sin rewarded ads reales y sin compras reales de packs (solo foundation y puntos de integración para iteraciones siguientes).

## 2026-04-21 (billing Android hotfix: eliminar copy hardcodeada en datasource)
- Android Billing: se elimina fallback textual hardcodeado (`"Monthly plan"` / `"Annual plan"`) del mapping `ProductDetails -> SubscriptionPlan` en `GooglePlaySubscriptionBillingDataSource`.
- El mapping robusto se mantiene: si Play no entrega `basePlanId` ni `title` utilizables, el título cae a `productId` técnico no vacío (sin copy visible embebida en data layer).
- Sin cambios de arquitectura ni de flujo: se conserva orden mensual/anual de catálogo, hardening de fases/precio y resolución de gestión de suscripción de la iteración previa.

## 2026-04-21 (billing Android productización corta: IDs + catálogo + gestión)
- Android Billing: se deja un punto único y explícito para IDs de suscripción Play en `KnownSubscriptionProducts` (`MONTHLY`/`ANNUAL` + `ordered`) y se reutiliza desde launcher + datasource para sustituir IDs reales con cambio mínimo y de bajo riesgo.
- Catálogo Play: `GooglePlaySubscriptionBillingDataSource` ahora consulta `ProductDetails` respetando orden canónico mensual→anual (`queryOrder`) en vez de set sin orden estable.
- Mapping `ProductDetails -> SubscriptionPlan`: se endurece la selección de `pricingPhase` para priorizar fases con `formattedPrice` y `billingPeriod` válidos; si el periodo no permite inferir tipo, se usa fallback por `productId` conocido.
- Mapping de título/plan: fallback de título alineado a datos técnicos de catálogo (`basePlanId`/`title` y, en ausencia, `productId`) para evitar labels vacíos sin introducir copy de producto en data layer.
- Gestión de suscripción: `SettingsViewModel` intenta primero usar `productId` real del catálogo actual para el estado activo (mensual/anual) y mantiene fallback a IDs conocidos si el catálogo no está disponible.

Checklist manual Play Console (activación real Android):
- Productos: crear 2 suscripciones activas (mensual/anual) con los IDs finales y al menos un base plan/offer comprable por testers.
- Testers: añadir cuentas en License Testing (Play Console) y en track interno/cerrado donde se distribuya el build.
- Track: publicar build firmada en Internal testing (o Closed testing) con `versionCode` nuevo; esperar propagación antes de probar compra.
- Firma/build: validar que el `applicationId` y la firma de subida corresponden a la app registrada en Play Console (mismo paquete del SKU).
- Cuenta Play en dispositivo: iniciar sesión con tester autorizado en Play Store, actualizar Play Store/Play Services y reinstalar build desde track para evitar caché de SKU antigua.

## 2026-04-21 (billing Android hardening: ProductDetails ausente)
- Android Billing (Google Play): se endurece `GooglePlaySubscriptionBillingDataSource` para que la ausencia de `ProductDetails` configurados, producto no encontrado o `offerToken` faltante no provoque crash.
- `launchPurchaseFlow(...)` ahora encapsula excepciones y devuelve `Result.failure(...)` de forma controlada cuando Play Console/catálogo no está correctamente configurado.
- Launcher Android de compra (`SubscriptionPurchaseLauncher.android.kt`) ahora envuelve la llamada al datasource con `runCatching`, garantizando salida tipada (`Success` / `Cancelled` / `Failed` / `Unsupported`) sin cierre inesperado de la app.
- UX/i18n: se mantiene el feedback existente de compra fallida (`subscriptionPurchaseFailed`) sin exponer mensajes técnicos raw al usuario.

# 2026-04

## 2026-04-28 (cierre analytics-funnels-v1: documentación y validación de wiring)
- Se actualiza `docs/context/analytics-events-v1.md` con estado vigente de eventos y params opcionales nuevos para funnels: `paywall_impression_id`, `unlock_flow_origin`, `origin_placement`.
- Se documenta catálogo cerrado de `unlock_flow_origin`: `direct_balance`, `paywall_rewarded`, `paywall_store`, `premium`, `unknown`.
- Se añaden ejemplos operativos para reconstrucción de funnels: paywall→rewarded→unlock, CTA rewarded con terminal completed/failed y CTA premium desde settings hacia purchase_started.
- Se ejecuta verificación global de wiring (`rg`) en `shared`, `composeApp` y `docs` para confirmar consistencia de naming snake_case/camelCase y presencia de params en contrato + emisión.
- Se valida que no se reintroduce tracking de `paywall_shown` desde `HoroscopeViewModel` ni desde `requireLunas` fuera del punto centralizado en `EconomyViewModel.onMoonPaywallShown`.
- Se confirma que en esta iteración no se modifican reglas de costes/límites de economía; el cierre es documental y de consistencia de analytics.

## 2026-04-06
- Rituales: implementada la base del flujo **Ritual del día** con arquitectura en capas (domain/data/presentation/composeApp).
- Domain: nuevos modelos `DailyRitualTheme`, `DailyRitualStepKind`, `DailyRitualStep`, `DailyRitualTemplate` y contrato `DailyRitualRepository`.
- Data: nuevo repositorio local `SettingsDailyRitualRepository` con 8 plantillas estáticas, selección diaria con variabilidad (evita repetir plantilla exacta del día previo y prioriza tema diferente), persistencia mínima (última fecha completada, racha, plantilla diaria seleccionada).
- Presentation/UI: `DailyRitualViewModel` + `DailyRitualUiState` y nueva `DailyRitualScreen` con estados intro/flujo/completado, validaciones por tipo de paso y resolución de ramas para `BinaryChoice`.
- Navegación: nuevo destino `Destination.DailyRitual` y conexión desde la card "Ritual del día" en la home de Rituales.
- Rituales: implementado MVP de **Hábitos** local/offline (sin backend) con 12 intenciones en pool local, selección diaria de 3 intenciones persistidas por fecha y estado completado del día.
- Progreso Hábitos: cada acción completada suma +1, ciclo de 60 puntos, reseteo automático del ciclo al completar objetivo y contador histórico de ciclos completados persistido en Settings.
- Presentation/UI Hábitos: nuevo `HabitsViewModel` + `HabitsUiState`, pantalla `HabitsScreen` con anillo de progreso simple, textos suaves de avance y lista de 3 intenciones con CTA para marcar/desmarcar.
- Navegación: nuevo destino `Destination.Habits` y conexión desde la card "Hábitos" en `RitualsPlaceholderScreen`.

Impacto arquitectónico:
- Sin cambios de módulos ni de patrón base. Se añade nueva vertical slice de feature dentro de la arquitectura actual.

Pendientes:
- Persistir respuestas de pasos (si se decide mostrar historial en futuras iteraciones).
- Ampliar plantillas y variaciones de ramas en más temas.
- Placeholder visual para insignias/logros de Hábitos (el dato ya está modelado como `completedCycles`).

## 2026-04-07
- Hábitos: añadida sincronización Firestore local-first en `users/{uid}/habits/current` con snapshot único (`todayDateIso`, intenciones seleccionadas/completadas, progreso, ciclos, `updatedAtEpochMillis`).
- Data: nuevo `SyncHabitsRepository` siguiendo patrón `LocalStore + RemoteStore + SyncEngine`, con pull corto best-effort en lectura/operaciones sensibles y push asíncrono tras mutaciones.
- Data local: `SettingsHabitsRepository` ahora persiste/expone snapshot completo + `updatedAtEpochMillis` para resolver conflictos por last-write-wins entre dispositivos.
- DI: `HabitsRepository` pasa a resolverse con `SyncHabitsRepository`, manteniendo `SettingsHabitsRepository` como base local/fallback.
- Docs Firestore actualizadas (schema, rules, examples) para reflejar la nueva colección de hábitos sincronizados.

Impacto arquitectónico:
- Sin cambios de módulos ni UX. Se extiende patrón de sync existente a la feature Hábitos.

Pendientes:
- Mantener MVP sin histórico diario ni listeners realtime (fuera de alcance actual).

## 2026-04-09
- i18n foundation (subfase infraestructura): añadidos `AppLanguage` + `AppLanguageRepository` en domain con lista estable de idiomas soportados (`es`, `en`, `pt`, `ru`, `fr`, `it`, `de`) y fallback explícito a español.
- Data local-first: nuevo `SettingsAppLanguageRepository` usando `multiplatform-settings` para persistencia local de idioma (`bwitch_language/current_language_code`).
- Resolución inicial de idioma: prioridad **guardado BWitch > locale del sistema soportado > español**, encapsulada en el repositorio.
- KMP locale hook: nuevo `SystemLanguageCodeProvider` expect/actual (Android/iOS) para leer idioma del sistema sin acoplar commonMain a APIs de plataforma.
- Estado global: nuevo `AppLanguageViewModel` en presentation con estado observable + acción de cambio de idioma.
- Integración mínima UI: selector reutilizable `LanguageSelectorSection` conectado en onboarding y ajustes; sin migración masiva de textos aún.
- Resources strategy: creado primer `composeResources/values/strings.xml` para el bloque de idioma y dejar trazado el camino de migración gradual a resources.

Impacto arquitectónico:
- Sin cambios de módulos ni patrón. Se añade vertical de localización respetando capas domain/data/presentation/composeApp.

Pendientes:
- Migrar textos pantalla-a-pantalla a Compose resources por idioma (`values-es`, `values-en`, etc.).
- Aplicar locale actual a toda la UI (titles, labels y contenido) conforme avance la migración de strings.
- Definir experiencia final de selector en onboarding (paso dedicado vs integrado) y ajustes (orden visual/UX final).

## 2026-04-09 (fase 2 i18n - aplicación real de locale)
- ⚠️ **Registro histórico (intento inicial, no estado vigente final):** se probó un bridge de locale runtime con `LocalAppLocale` (expect/actual Android+iOS) más `AppLocaleEnvironment` para intentar que el idioma guardado en `AppLanguage` impactara directamente `stringResource(...)`.
- En ese intento también se consolidó la migración mínima de textos (onboarding, ajustes, selector idioma y labels globales de tabs) y los recursos base `strings.xml` para `es`, `en`, `pt`, `ru`, `fr`, `it`, `de`.
- Este bloque queda **supersedido** por la sección de hardening/rollback del mismo día y no debe leerse como comportamiento vigente.

Impacto arquitectónico:
- Sin cambios de módulos ni DI. Se completa el puente UI para que `AppLanguage` sea fuente de verdad también en recursos Compose.

Pendientes:
- Migrar títulos de navegación (`Destination.title`) y el resto de pantallas aún hardcodeadas.
- Definir si iOS debe forzar reinicio completo de escena para garantizar cambio instantáneo en todos los componentes nativos fuera de Compose.

## 2026-04-09 (fase 2 i18n - hardening / rollback de runtime locale hack)
- Se elimina la implementación previa de `LocalAppLocale` que mutaba estado global de plataforma desde Compose (`Locale.setDefault + updateConfiguration` en Android y `AppleLanguages` en iOS).
- `AppLocaleEnvironment` se simplifica a un boundary de recomposición (`key(languageCode)`) sin side effects globales.
- Se mantiene intacta la base i18n ya válida: `AppLanguage`, persistencia local, `AppLanguageViewModel`, selector de idioma y bloque mínimo de strings traducidos por idioma.

Garantía actual:
- El estado vigente final usa recomposición controlada por `key(languageCode)` en `AppLocaleEnvironment`.
- No hay runtime locale forcing de plataforma (sin `Locale.setDefault`, sin `resources.updateConfiguration`, sin `AppleLanguages`).
- El cambio de idioma actualiza estado y recompone UI.
- **Limitación abierta:** todavía no está garantizado que `stringResource(...)` siga el idioma BWitch en todas las plataformas hasta definir una estrategia robusta/oficial para Compose MPP.

## 2026-04-09 (fase 2 i18n - consistencia temporal de navegación)
- Se audita la inconsistencia visual y se confirma una causa mixta: parte de la UI usaba `stringResource(...)` (atado al locale efectivo del sistema) y otra parte seguía en `Destination.title` hardcodeado en español.
- Se aplica estrategia conservadora temporal (Opción A) para navegación principal: títulos de top bar y labels de bottom tabs vuelven a usar una única fuente estable (`Destination.title`) mientras se define una solución robusta de runtime locale en Compose MPP.
- Se evita seguir extendiendo migración de textos en esta iteración; se mantiene infraestructura i18n (selector + persistencia + estado global) sin hacks nuevos de locale runtime.

Estado real final:
- La selección de idioma BWitch sigue persistiéndose y recomponiendo UI.
- La navegación principal ahora evita mezcla visual entre inglés/español por origen de strings distinto.
- `stringResource(...)` continúa dependiendo del locale efectivo del sistema hasta resolver estrategia oficial cross-platform.

## 2026-04-09 (fase 3 i18n - AppStrings como fuente principal)
- Se implementa una capa tipada de textos en `commonMain` (`AppStrings`) con secciones: `AuthStrings`, `OnboardingStrings`, `SettingsStrings`, `NavigationStrings` y `CommonStrings`.
- Se añade resolvedor por idioma (`resolveAppStrings`) conectado a `AppLanguage` y cobertura para `es`, `en`, `pt`, `ru`, `fr`, `it`, `de`, con fallback explícito a español (`fallbackAppStrings`).
- Se crea `LocalAppStrings` y se provee desde `App()` usando el estado real de `AppLanguageViewModel`; `AppLocaleEnvironment` se mantiene solo como boundary de recomposición sin forzar locale de plataforma.
- Migración completa (sin mezcla con `stringResource(...)`) en zonas acordadas:
  - `AuthScreen`
  - `OnboardingProfileScreen`
  - `SettingsScreen`
  - Títulos de navegación principales (`AppScaffold.title` en `AppRoot`)
  - Labels de bottom tabs principales

Impacto arquitectónico:
- Sin cambios de módulos ni dependencias nuevas. Se consolida una arquitectura de i18n app-driven para UI migrada, independiente del locale efectivo del sistema.

Pendientes:
- Migrar el resto de pantallas a `LocalAppStrings` de forma incremental.
- Reducir/eliminar `Destination.title` como fuente visual directa una vez migrada toda la navegación/feature copy.

## 2026-04-09 (fase 3 i18n - iteración astrología/perfil)
- Migradas a `AppStrings + LocalAppStrings` las copias visibles de `ProfileScreen` y la pantalla raíz de `AstrologyScreen`.
- Migradas también `BirthChartScreen` y `HoroscopeScreen` en el mismo lote (títulos, CTAs, subcopy y labels visibles), evitando mezclar `stringResource(...)` en estas pantallas.
- `AppStrings` se amplía con secciones tipadas para este bloque (`profile`, `astrologyHome`, `birthChart`, `horoscope`, `zodiac`) con cobertura de idiomas `es`, `en`, `pt`, `ru`, `fr`, `it`, `de` y fallback en español.

## 2026-04-09 (horóscopo multiidioma end-to-end)
- Domain: `DailyHoroscope` incorpora `languageCode` y los contratos de horóscopo (`observe/get/pull`) pasan `languageCode` explícito para evitar defaults implícitos.
- Presentation: `HoroscopeViewModel` observa `AppLanguage` (fuente de verdad) y reinicia resolución de horóscopo por idioma al cambiar idioma o signo, manteniendo semántica de loading/error/refresh.
- Data local: cache Settings de horóscopo migra de `horoscope_daily_{date}_{sign}` a `horoscope_daily_{date}_{sign}_{lang}` con lectura legacy sin idioma como fallback explícito.
- Sync Firestore: `SyncHoroscopeDailyRepository` prioriza `horoscopeDaily/{date}/signs/{sign}/langs/{lang}` y solo cae al doc legacy `.../signs/{sign}` cuando no existe variante por idioma.
- Política de fallback explícita:
  - remoto legacy (`.../signs/{sign}`) se permite como fallback cross-language y se cachea bajo la clave del idioma solicitado para no romper `observe/get` por idioma;
  - local legacy (`horoscope_daily_{date}_{sign}`) solo se reutiliza para solicitudes `es` para evitar etiquetar en disco contenido histórico como otro idioma.
- Pull marker: persistencia del último pull se vuelve por idioma (`last_pulled_date_iso_{lang}`) para no bloquear refresh automático al cambiar lenguaje en el mismo día.
- Functions: `HoroscopeGenerator` escribe también `languageCode` en cada documento generado para facilitar trazabilidad del contenido.
- Docs Firestore: añadido schema de la ruta canónica multiidioma y fallback legacy.

Impacto arquitectónico:
- Sin cambios de módulos ni patrón base. Se completa propagación de idioma en la vertical de horóscopo (domain/data/presentation + backend) respetando Clean Architecture.

Pendientes:
- Configurar en backend `HOROSCOPE_USE_LANGS=true` y `ACTIVE_LANGS` con el set deseado en producción.
- Backfill/re-generación por idioma para fechas relevantes si se quiere evitar fallback a documentos legacy en español.
- No aplica todavía a esencia natal ni sinastría (fuera de alcance de esta iteración).

## 2026-04-10 (Esencia Natal multiidioma end-to-end)
- Presentation: `BirthChartViewModel` ahora observa `AppLanguage` y propaga `languageCode` explícito al flujo de generación/guardado de esencia, sin depender de locale de plataforma.
- Domain: modelos de esencia (`BirthEssenceInput/Reading/Draft/Profile`) incorporan `languageCode` como parte del contrato para trazabilidad del idioma de contenido generado.
- Data local/sync: persistencia de esencia (Settings + Firestore `users/{uid}/birthEssence/current`) ahora guarda `languageCode`; documentos legacy sin idioma conservan fallback explícito a `es`.
- Functions: callable `birthEssenceGenerate` acepta `languageCode`/`lang`, valida contra idiomas soportados, fuerza prompt en ese idioma y devuelve `languageCode` en la respuesta.
- UI: visualización de arquetipo en `BirthChartScreen`, `ProfileScreen` y tarjeta de share usa nombre localizado según `languageCode` de la esencia.
- Tests: se añaden pruebas de dominio/data para verificar propagación de idioma y compatibilidad legacy.

Impacto arquitectónico:
- Sin cambios de módulos ni patrón base. Se extiende el patrón multiidioma ya aplicado en Horóscopo a la vertical de Esencia Natal.

Pendientes:
- Si se requiere historial multiidioma por usuario, evaluar en una iteración posterior un storage por idioma (actualmente se mantiene un único `current` con idioma explícito).

## 2026-04-20 (Settings iteración: estado + persistencia local de notificaciones)
- Ajustes: se extrae estado principal fuera de `SettingsScreen` y se introduce `SettingsViewModel` + `SettingsUiState` ligeros en `shared/presentation`.
- Notificaciones: nuevo modelo de dominio `NotificationSettings` y contrato `NotificationSettingsRepository`.
- Persistencia local: nueva implementación `SettingsNotificationSettingsRepository` (multiplatform-settings) para guardar `globalEnabled`, `dailyHoroscopeEnabled`, `ritualOfDayEnabled` y `habitsEnabled`.
- UI: `SettingsScreen` deja de usar `rememberSaveable` para switches de notificaciones y consume estado/handlers de `SettingsViewModel`.
- Idioma: se mantiene wiring real existente con `AppLanguageViewModel` para cambiar idioma; además `SettingsViewModel` observa idioma actual para mostrarlo en su estado.
- Logout: se mantiene comportamiento actual (`SessionViewModel.signOut()` + `ClearLocalUserDataUseCase`) sin introducir cambios de alcance.
- Placeholders: se conservan sin backend real para suscripción/restore/support/report/privacy/terms/delete account.

Impacto arquitectónico:
- Sin cambios de módulos ni patrón base. Se refuerza separación UI/estado en Settings y se añade persistencia local de preferencias de notificación siguiendo arquitectura actual.

Pendientes:
- Integrar permisos del sistema, push y scheduling real de notificaciones.
- Conectar billing real, soporte real, legales reales y delete account backend en iteraciones futuras.

## 2026-04-20 (Settings follow-up: fix de compilación/imports)
- Presentation/Settings: corregido import faltante de Flow (`collect`) en `SettingsViewModel` para evitar fallo de compilación tras refactor.
- UI/Settings: limpieza menor en `SettingsScreen` y ajuste de `appVersion` a valor memorizado local (`remember { getAppVersionLabel() }`) manteniendo el flujo actual de sincronización con ViewModel.
- Error state: `SettingsScreen` ahora muestra `uiState.error` en `SnackbarHost` con un manejo mínimo y no invasivo.

Impacto arquitectónico:
- Sin cambios de arquitectura ni de alcance funcional; hardening del refactor previo.

## 2026-04-27 (Functions: hardening weekly/monthly + backfill admin-safe)
- Functions/Horoscope periods: scheduler semanal y mensual endurecidos para separar fases canonical/traducciones y bloquear traducciones por `periodKey+sign` únicamente cuando el canónico falla en esa ejecución.
- Functions/Horoscope periods: ventana configurable por entorno con mínimo conservador `2` (`current + next`) mediante:
  - `WEEKLY_WINDOW_SIZE`
  - `MONTHLY_WINDOW_SIZE`
- Functions/Admin: nueva callable `backfillPeriodHoroscopes` protegida con:
  - autenticación requerida,
  - allowlist de UIDs (`BACKFILL_ADMIN_UIDS`),
  - App Check (según `ALLOW_UNVERIFIED_APPCHECK_IN_DEV`),
  - límites anti-coste por rango (`max 8` semanas / `max 6` meses).
- Nota operativa:
  - Si `ACTIVE_LANGS` contiene solo `es`, el backfill no rompe: genera/valida canónico y omite traducciones al no haber idiomas objetivo distintos de `es`.

## 2026-04-20 (Settings suscripción foundation sin billing real)
- Domain/Settings: se añade modelo explícito de suscripción (`SubscriptionStatus`) y contrato `SubscriptionRepository` para desacoplar Settings de la futura integración Store/Billing.
- Domain/Settings: nuevos casos de uso `ObserveSubscriptionStatusUseCase`, `GetSubscriptionStatusUseCase` y `RestorePurchasesUseCase`.
- Data/Settings: nueva implementación local `SettingsSubscriptionRepository` (multiplatform-settings) que expone estado persistido de suscripción y una restauración mock controlada (`Restored` / `NoPurchasesFound`) sin SDKs de plataforma.
- Data/Settings (follow-up): `SettingsSubscriptionRepository` ahora modela también un estado restaurable persistido (`restorable_subscription_status`) y `restorePurchases()` promueve ese estado al actual cuando es activo, para simular un restore local coherente.
- Presentation/Settings: `SettingsViewModel` ahora observa estado de suscripción, decide CTA principal (`Subscribe`/`Manage`) y ejecuta flujo de restaurar compras desde el modelo (sin placeholders directos en UI).
- UI/Settings: sección "Compras y suscripción" muestra estado de suscripción real del modelo (`unknown/inactive/monthly/annual`), CTA dinámico según estado y acción de restaurar conectada al ViewModel.
- i18n: ampliado `SettingsStrings` con nuevos textos de estado/feedback de suscripción para `es`, `en`, `pt`, `ru`, `fr`, `it`, `de`.
- DI: registrados repositorio y casos de uso nuevos en módulos Koin existentes.

Impacto arquitectónico:
- Sin cambios de módulos ni dependencias nuevas. Se prepara un boundary limpio para reemplazar el repositorio local por Billing real en una iteración posterior.

Pendientes:
- Sustituir `SettingsSubscriptionRepository` por implementación real con Google Play Billing / StoreKit.
- Añadir validación server-side de receipts, restore cross-device y sincronización backend del entitlement.
- Conectar navegación/acciones reales de alta y gestión de suscripción.

## 2026-04-20 (i18n global audit - fase 1, hardening de mensajes visibles)
- Auditoría focalizada en flujos centrales (auth, sinastría, esencia natal, tarot y oráculo) para detectar textos visibles aún no controlados por i18n app-driven.
- Auth (`AuthScreen`): se evita exponer mensajes técnicos/raw de proveedor al usuario; se añade fallback de error tipado en `AppStrings.auth` para todos los idiomas soportados (`es`, `en`, `pt`, `ru`, `fr`, `it`, `de`).
- Sinastría (`SynastryScreen`): fallback de error pasa a mensaje i18n genérico en vez de mostrar texto raw de backend.
- Esencia natal (`BirthChartScreen`): fallback desconocido del mapper de errores pasa a copy i18n (`generateErrorFallback`) en lugar de `else -> this`.
- Tarot (`TarotScreen`): bloque de error evita renderizar mensaje raw y usa fallback i18n consistente.
- Oráculo (`OracleScreen`): caso `RawBackendMessage` deja de mostrar texto sin traducir y usa fallback i18n de error conocido.

Impacto:
- Mejora de consistencia i18n sin refactor masivo ni cambios de arquitectura.
- Se reduce la probabilidad de mostrar mensajes mezclados por idioma o técnicos al usuario final.

Pendientes fase 2 sugeridos:
- Extender normalización de errores en ViewModels/repositories (mapear códigos de dominio en origen en vez de en UI).
- Revisar pantallas secundarias para convertir más mensajes técnicos heredados a claves de i18n tipadas.

## 2026-04-20 (i18n global audit - fase 2, copy visible en pantallas principales)
- Auditoría rápida en pantallas prioritarias (Auth, Profile, Astrology/Birth Chart y Oracle) para detectar hardcodes visibles y copy fuera de `AppStrings`.
- Auth: eliminado hardcode visual del título principal (`"BWitch"`) y unificado uso de `AppStrings` para el nombre de app.
- Oracle: eliminados hardcodes de bullets en listas de “hacer/evitar”; ahora el render usa formato tipado en `AppStrings`.
- Profile: eliminado hardcode de contador de lunas (`"🌙 x N"`), movido a formato i18n en `AppStrings.profile`.
- Birth Chart share: eliminada firma hardcodeada (`"BWitch ✨"`) en tarjeta de compartir, ahora resuelta desde `AppStrings.birthChart`.
- `AppStrings`: ampliado modelo tipado con nuevas claves y cobertura completa en `es`, `en`, `pt`, `ru`, `fr`, `it`, `de`.

Impacto:
- Mejora directa de consistencia de copy y reducción de textos literales visibles al usuario en flujos de alto tráfico.
- Sin refactor masivo ni cambios de arquitectura i18n: se mantiene el patrón app-driven existente.

Pendientes fase 3 sugeridos:
- Continuar barrido en pantallas secundarias de Guide/Rituals para eliminar format strings visuales residuales.
- Evaluar normalizar otros formatos compuestos (`prefix + dato`) en claves tipadas reutilizables para máxima consistencia cross-screen.

## 2026-04-22 (KMP economy migration fase 4 - Oráculo cliente)
- Presentation/Oráculo: `OracleAskViewModel` cambia a estrategia backend-first para economy, eliminando suposición de ad unlock como camino principal y manteniendo compatibilidad temporal solo ante señales legacy explícitas (`rewardedProof` / `AD_UNLOCK`).
- Error mapping Oráculo: se separan helpers para distinguir restricciones económicas reales (insufficient moons), señal legacy de ad unlock y errores genéricos/temporales, evitando tratar límites genéricos como "falta de lunas".
- Post-consulta Oráculo: se añade refresh best-effort de snapshot de economy backend tras éxito y también tras error económico real para reducir desalineación entre estado visible y source of truth server-side.
- Corrección funcional: el refresh de economy en Oráculo deja de ser no-op y ahora persiste un snapshot mínimo (`economyBalance`) en `OracleAskUiState`.
- DI + tests: `presentationModule` actualiza inyección de `OracleAskViewModel` con `EconomyRepository`; se amplían tests de `OracleAskViewModel` para cubrir éxito con refresh, error económico con refresh y señal legacy sin refresh.

Impacto:
- Cambio incremental y acotado en cliente KMP (sin backend), alineado con migración gradual economy v2 y convivencia temporal con modo legacy.
- Sin cambios de arquitectura de capas ni contratos públicos de dominio/API.

Pendientes:
- Migrar cliente de Esencia natal a la misma estrategia backend-first economy en la siguiente fase.
- Retirar fallback legacy de ad unlock en Oráculo/Tarot cuando el flag backend legacy quede definitivamente OFF.

## 2026-04-20 (i18n global audit - fase 3, pantallas secundarias Guide/Rituals)
- Auditoría focalizada en `Guide` y flujos de `Rituals` (Daily Ritual, Habits y catálogo/detalle) para detectar formatos visuales inline y copy residual fuera de `AppStrings`.
- Guide/Péndulo: eliminado formato compuesto inline para resultado (`"${prefix} ${answer}"`) y migrado a clave tipada `resultSentenceFormat` en `AppStrings.pendulum`.
- Ritual detalle (catálogo): eliminado bullet inline en lista de materiales (`"• $item"`) y migrado a clave tipada `detailMaterialBulletFormat` en `AppStrings.ritualCatalog`.
- Cobertura i18n completa: nuevas claves añadidas en los 7 idiomas soportados (`es`, `en`, `pt`, `ru`, `fr`, `it`, `de`) sin introducir literales nuevos en UI.

## 2026-04-21 (Settings billing Android purchase flow - Google Play)
- Android billing: `GooglePlaySubscriptionBillingDataSource` extiende la integración foundation para compra real con `launchBillingFlow`, listener de resultados de compra y consulta de productos suscripción vía `queryProductDetails`.
- Android billing: el flujo de compra ahora resuelve `ProductDetails` para catálogo configurado (`monthly` / `annual`) y usa `offerToken` antes de lanzar Play UI.
- Presentation/Settings: `SettingsViewModel` introduce `SettingsUiEffect` para solicitar launch de compra desde UI (sin pasar `Activity` al ViewModel), más manejo básico de outcomes (`success/cancelled/unsupported/failed`).
- UI/Settings: la acción de suscripción ahora abre selector simple de plan (mensual/anual), delega launch real en Android y refresca estado de suscripción tras compra exitosa vía `getSubscriptionStatus()`.
- KMP/platform: se añade `SubscriptionPurchaseLauncher` expect/actual (`androidMain` compra real, `iosMain` fallback unsupported) para no romper iOS y mantener boundary multiplataforma limpio.
- DI Android: `platformModule` registra el datasource concreto de Google Play además del binding por interfaz para poder usar launch real desde la capa UI de plataforma.

Impacto arquitectónico:
- Se mantiene Clean Architecture por capas: dominio/repositorio siguen limitados a estado/restore, mientras el purchase flow queda encapsulado en boundary de plataforma Android + datasource de data.

Pendientes:
- Validación backend de receipts, ack/consume avanzado, estado completo de suscripciones (grace/expired/hold), paywall final y StoreKit iOS.

## 2026-04-21 (Settings billing hardening corto)
- Purchase flow Android: listener de compra endurecido; éxito solo cuando existe al menos una compra relevante en estado `PURCHASED` para SKUs conocidas.
- Billing datasource: `launchPurchaseFlow` ya no mantiene el `Mutex` durante toda la espera del resultado del purchase flow; el await del listener se ejecuta fuera del lock.
- Settings/UI: se separan labels de plan vs labels de estado (`subscriptionPlanMonthlyLabel`, `subscriptionPlanAnnualLabel`) en el diálogo de selección.
- UX errores: se eliminan mensajes técnicos/raw visibles en el flujo de compra y se usa feedback controlado tipado (`subscriptionPurchaseFailed`) en lugar de propagar mensajes internos a UI.

Impacto:
- Mejora de consistencia visual cross-screen en formatos compuestos secundarios, manteniendo riesgo bajo y cambios acotados.
- Sin refactor masivo ni cambios de arquitectura; se mantiene enfoque incremental app-driven con `AppStrings`.

Pendientes fase 4 sugeridos:
- Continuar barrido en secundarios restantes fuera de Guide/Rituals (p. ej. overlays/utilidades) para formatos visuales heredados.
- Valorar centralizar helper de placeholders (`%s`/`%d`) para evitar duplicación de utilidades locales por pantalla.

## 2026-04-20 (i18n global audit - fase 4 final, overlays/share launcher)
- Barrido final en componentes secundarios de share/overlay de Esencia Natal (commonMain + Android/iOS) para localizar copy visible residual fuera de `AppStrings`.
- Share launcher Android: eliminados literales hardcodeados en español para título del chooser y errores de captura/serialización; ahora se inyectan desde `AppStrings.birthChart`.
- Share launcher iOS: eliminado mensaje hardcodeado de "not implemented" en español y migrado a clave i18n en `AppStrings.birthChart`.
- `BirthEssenceShareLauncher` (expect/actual): actualizado contrato para recibir `BirthChartStrings` y mantener toda la copy visible en la capa i18n app-driven.
- `AppStrings.birthChart`: añadidas claves específicas de share launcher (`shareChooserTitle`, errores de captura/serialización y disponibilidad de plataforma) con traducción completa `es/en/pt/ru/fr/it/de`.

Impacto:
- Cierre de auditoría i18n con quick wins de bajo riesgo en superficie secundaria visible al usuario.
- Sin cambios de arquitectura ni refactor masivo.

## 2026-04-20 (Billing foundation real - Android Play Billing MVP)
- Settings/Suscripción: se reemplaza la implementación local fake por `BillingBackedSubscriptionRepository`, manteniendo el contrato de dominio `SubscriptionRepository` para no romper UI/ViewModels.
- Data/Billing boundary: nuevo `SubscriptionBillingDataSource` en `shared:data` como frontera explícita de plataforma (sin detalles de Google Play en domain/presentation).
- Android real integration: se añade `GooglePlaySubscriptionBillingDataSource` con Google Play Billing (`billing-ktx`) para consultar compras activas de tipo `SUBS` y mapearlas a `SubscriptionStatus` (`Inactive`, `ActiveMonthly`, `ActiveAnnual`).
- Restore purchases: `restorePurchases()` deja de ser mock en Android y ahora reutiliza consulta real de compras activas para restauración básica (sin backend de receipts aún).
- Fallback seguro multiplataforma: `UnsupportedSubscriptionBillingDataSource` queda como implementación por defecto (iOS/otros), conservando comportamiento controlado sin romper Settings.
- DI: `dataKoinModule` registra fallback por defecto y `platformModule` Android sobrescribe el binding con la implementación real de Play Billing.

Impacto arquitectónico:
- Sin cambios en contratos de dominio ni refactor gigante de presentación/UI.
- Boundary de billing preparado para integrar StoreKit en iOS y/o validación backend en iteraciones siguientes.

Pendientes siguientes fases:
- Reemplazar IDs placeholder de productos por IDs reales de Play Console y añadir catálogo remoto/configurable.
- Conectar flujo real de compra/gestión (launch billing flow / deep link a gestión).
- Añadir validación server-side de receipts/entitlements y sincronización cross-device.
- Implementar StoreKit (iOS) detrás del mismo `SubscriptionBillingDataSource`.

## 2026-04-20 (Billing foundation hardening)
- Android billing datasource: se endurece manejo de errores técnicos en setup/query de Play Billing; ya no se degradan como éxito silencioso a `Inactive`.
- Subscription status criteria: para esta fase foundation se considera activo cualquier `PurchaseState.PURCHASED` (sin exigir `isAcknowledged`) para evitar falsos negativos temporales antes de cerrar flujo completo de compra/ack.
- Fallback multiplataforma: se mantiene `UnsupportedSubscriptionBillingDataSource` como comportamiento honesto/controlado fuera de Android (sin romper Settings ni desarrollo local).
- Tests: se agregan pruebas unitarias de `BillingBackedSubscriptionRepository` para cobertura mínima de resolución por billing, fallback persistido ante error y restore en plataforma no soportada.
