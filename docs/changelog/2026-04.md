# 2026-04

## 2026-04-06
- Rituales: implementada la base del flujo **Ritual del día** con arquitectura en capas (domain/data/presentation/composeApp).
- Domain: nuevos modelos `DailyRitualTheme`, `DailyRitualStepKind`, `DailyRitualStep`, `DailyRitualTemplate` y contrato `DailyRitualRepository`.
- Data: nuevo repositorio local `SettingsDailyRitualRepository` con 8 plantillas estáticas, selección diaria con variabilidad (evita repetir plantilla exacta del día previo y prioriza tema diferente), persistencia mínima (última fecha completada, racha, plantilla diaria seleccionada).
- Presentation/UI: `DailyRitualViewModel` + `DailyRitualUiState` y nueva `DailyRitualScreen` con estados intro/flujo/completado, validaciones por tipo de paso y resolución de ramas para `BinaryChoice`.
- Navegación: nuevo destino `Destination.DailyRitual` y conexión desde la card "Ritual del día" en la home de Rituales.
- Rituales: implementado MVP de **Hábitos** local/offline (sin backend) con 12 intenciones en pool local, selección diaria de 3 intenciones persistidas por fecha y estado completado del día.
- Progreso Hábitos: cada acción completada suma +1, ciclo de 60 puntos, reseteo automático del ciclo al completar objetivo y contador histórico de ciclos completados persistido en Settings.
- Presentation/UI Hábitos: nuevo `HabitsViewModel` + `HabitsUiState`, pantalla `HabitsScreen` con anillo de progreso simple, textos suaves de avance y lista de 3 intenciones con CTA para marcar/desmarcar.
- Navegación: nuevo destino `Destination.Habits` y conexión desde la card "Hábitos" en `RitualsPlaceholderScreen`.

Impacto arquitectónico:
- Sin cambios de módulos ni de patrón base. Se añade nueva vertical slice de feature dentro de la arquitectura actual.

Pendientes:
- Persistir respuestas de pasos (si se decide mostrar historial en futuras iteraciones).
- Ampliar plantillas y variaciones de ramas en más temas.
- Placeholder visual para insignias/logros de Hábitos (el dato ya está modelado como `completedCycles`).

## 2026-04-07
- Hábitos: añadida sincronización Firestore local-first en `users/{uid}/habits/current` con snapshot único (`todayDateIso`, intenciones seleccionadas/completadas, progreso, ciclos, `updatedAtEpochMillis`).
- Data: nuevo `SyncHabitsRepository` siguiendo patrón `LocalStore + RemoteStore + SyncEngine`, con pull corto best-effort en lectura/operaciones sensibles y push asíncrono tras mutaciones.
- Data local: `SettingsHabitsRepository` ahora persiste/expone snapshot completo + `updatedAtEpochMillis` para resolver conflictos por last-write-wins entre dispositivos.
- DI: `HabitsRepository` pasa a resolverse con `SyncHabitsRepository`, manteniendo `SettingsHabitsRepository` como base local/fallback.
- Docs Firestore actualizadas (schema, rules, examples) para reflejar la nueva colección de hábitos sincronizados.

Impacto arquitectónico:
- Sin cambios de módulos ni UX. Se extiende patrón de sync existente a la feature Hábitos.

Pendientes:
- Mantener MVP sin histórico diario ni listeners realtime (fuera de alcance actual).

## 2026-04-09
- i18n foundation (subfase infraestructura): añadidos `AppLanguage` + `AppLanguageRepository` en domain con lista estable de idiomas soportados (`es`, `en`, `pt`, `ru`, `fr`, `it`, `de`) y fallback explícito a español.
- Data local-first: nuevo `SettingsAppLanguageRepository` usando `multiplatform-settings` para persistencia local de idioma (`bwitch_language/current_language_code`).
- Resolución inicial de idioma: prioridad **guardado BWitch > locale del sistema soportado > español**, encapsulada en el repositorio.
- KMP locale hook: nuevo `SystemLanguageCodeProvider` expect/actual (Android/iOS) para leer idioma del sistema sin acoplar commonMain a APIs de plataforma.
- Estado global: nuevo `AppLanguageViewModel` en presentation con estado observable + acción de cambio de idioma.
- Integración mínima UI: selector reutilizable `LanguageSelectorSection` conectado en onboarding y ajustes; sin migración masiva de textos aún.
- Resources strategy: creado primer `composeResources/values/strings.xml` para el bloque de idioma y dejar trazado el camino de migración gradual a resources.

Impacto arquitectónico:
- Sin cambios de módulos ni patrón. Se añade vertical de localización respetando capas domain/data/presentation/composeApp.

Pendientes:
- Migrar textos pantalla-a-pantalla a Compose resources por idioma (`values-es`, `values-en`, etc.).
- Aplicar locale actual a toda la UI (titles, labels y contenido) conforme avance la migración de strings.
- Definir experiencia final de selector en onboarding (paso dedicado vs integrado) y ajustes (orden visual/UX final).

## 2026-04-09 (fase 2 i18n - aplicación real de locale)
- ⚠️ **Registro histórico (intento inicial, no estado vigente final):** se probó un bridge de locale runtime con `LocalAppLocale` (expect/actual Android+iOS) más `AppLocaleEnvironment` para intentar que el idioma guardado en `AppLanguage` impactara directamente `stringResource(...)`.
- En ese intento también se consolidó la migración mínima de textos (onboarding, ajustes, selector idioma y labels globales de tabs) y los recursos base `strings.xml` para `es`, `en`, `pt`, `ru`, `fr`, `it`, `de`.
- Este bloque queda **supersedido** por la sección de hardening/rollback del mismo día y no debe leerse como comportamiento vigente.

Impacto arquitectónico:
- Sin cambios de módulos ni DI. Se completa el puente UI para que `AppLanguage` sea fuente de verdad también en recursos Compose.

Pendientes:
- Migrar títulos de navegación (`Destination.title`) y el resto de pantallas aún hardcodeadas.
- Definir si iOS debe forzar reinicio completo de escena para garantizar cambio instantáneo en todos los componentes nativos fuera de Compose.

## 2026-04-09 (fase 2 i18n - hardening / rollback de runtime locale hack)
- Se elimina la implementación previa de `LocalAppLocale` que mutaba estado global de plataforma desde Compose (`Locale.setDefault + updateConfiguration` en Android y `AppleLanguages` en iOS).
- `AppLocaleEnvironment` se simplifica a un boundary de recomposición (`key(languageCode)`) sin side effects globales.
- Se mantiene intacta la base i18n ya válida: `AppLanguage`, persistencia local, `AppLanguageViewModel`, selector de idioma y bloque mínimo de strings traducidos por idioma.

Garantía actual:
- El estado vigente final usa recomposición controlada por `key(languageCode)` en `AppLocaleEnvironment`.
- No hay runtime locale forcing de plataforma (sin `Locale.setDefault`, sin `resources.updateConfiguration`, sin `AppleLanguages`).
- El cambio de idioma actualiza estado y recompone UI.
- **Limitación abierta:** todavía no está garantizado que `stringResource(...)` siga el idioma BWitch en todas las plataformas hasta definir una estrategia robusta/oficial para Compose MPP.

## 2026-04-09 (fase 2 i18n - consistencia temporal de navegación)
- Se audita la inconsistencia visual y se confirma una causa mixta: parte de la UI usaba `stringResource(...)` (atado al locale efectivo del sistema) y otra parte seguía en `Destination.title` hardcodeado en español.
- Se aplica estrategia conservadora temporal (Opción A) para navegación principal: títulos de top bar y labels de bottom tabs vuelven a usar una única fuente estable (`Destination.title`) mientras se define una solución robusta de runtime locale en Compose MPP.
- Se evita seguir extendiendo migración de textos en esta iteración; se mantiene infraestructura i18n (selector + persistencia + estado global) sin hacks nuevos de locale runtime.

Estado real final:
- La selección de idioma BWitch sigue persistiéndose y recomponiendo UI.
- La navegación principal ahora evita mezcla visual entre inglés/español por origen de strings distinto.
- `stringResource(...)` continúa dependiendo del locale efectivo del sistema hasta resolver estrategia oficial cross-platform.
